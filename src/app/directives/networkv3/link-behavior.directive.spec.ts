import { LinkBehaviorDirective } from './link-behavior.directive';

describe('LinkBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
