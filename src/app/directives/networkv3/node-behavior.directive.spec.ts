import { NodeBehaviorDirective } from './node-behavior.directive';

describe('NodeBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new NodeBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
