import { DragBehaviorDirective } from './drag-behavior.directive';

describe('DragBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new DragBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
